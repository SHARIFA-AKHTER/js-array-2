//1. Create an array of books containing different book.
let books =["To kill a Mockinbird","1984","Moby-Dick","JavScript: The Good Parts","The Great Gatsby"];
// console.log(books);


//2. Use the includes method to check if the array contains a javascript book.
// console.log(books.includes('JavScript: The Good Parts'));

// 3. Print a message to the console indicating whether the element is present in the array or not.
     if(books.includes){
         console.log("the array contains a javascript book");
     }
    else{
        console.log("the array does not contains a javascript book");
    }